server:
  port: 8080

spring:
  profiles:
    active: local
    include: aws, ethereum

---

cloud:
  aws:
    stack:
      auto: false
    region:
      static: ap-northeast-2
    credentials:
      instance-profile: true
    s3:
      bucket: blockai-bucket
